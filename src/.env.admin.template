# Aries Agents URL
ISSUER_AGENT_URL=http://172.51.42.23:8021   #IP and port of server where Issuer Agent is running
HOLDER_AGENT_URL=http://172.51.42.23:8021   #IP and port of server where Issuer Agent is running (this is to enable holder ops on issuer)

# Database Server IP
DATABASE_ADDRESS=192.168.12.98   #IP of server where Mongo Database is running
DATABASE_PORT=27017   #External port of docker db container

# Administration Handler URL
HANDLER_ADMIN_URL=http://192.168.12.98:4800   #IP and port of server where Admin Handler is running

# Encode/Decode Key
KEY=******   #Encode/decode key for authentication endpoints